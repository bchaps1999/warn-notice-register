# Source-first rebuild contract

Status: work in progress, 2026-09-22. The objective is the best defensible WARN dataset we can build from preserved source evidence, with a rebuild another person can verify. Matching the previously published database is **not** an acceptance criterion. It is a diagnostic comparison that can reveal missing sources or regressions, but it can also preserve old parsing, identity, and classification errors.

## Layers of reproducibility

1. **Inputs.** Preserve exact bytes of state captures, historical backfills, and agency documents in a checksum-verified bundle. Record capture date and source URL for each input. A mixed-time bundle is a valid experiment, but not a contemporaneous snapshot.
2. **Source interpretation.** Every canonical notice must trace to source rows or documents. Keep filed values and their roles (notice date, effective-date start/end, worksite, mailing address, per-site workers) separate from inferred values. Record date precision and the evidence for each inference. Do not fabricate a day, place, or worker allocation.
3. **Event identity and merging.** Prefer source filing/event IDs where trustworthy; model phases, amendments, multi-site filings, and duplicates explicitly. If distinct source rows collide under one key, quarantine them with a reason and source references. Do not silently collapse them or discard them to make old totals match.
4. **Derived decisions.** Pin reference datasets, human decisions, prompt and model versions, rendered inputs, and validation rules. Replaying existing decisions must require no network or model calls. A new model decision is a separate, reviewable experiment; it does not overwrite its evidence or the prior answer.
5. **Outputs.** From a fresh checkout and pinned inputs, generate the database, CSVs, and site data in an isolated directory. Pass `warnlive build-site --as-of YYYY-MM-DD` so site metadata and trailing windows do not depend on the wall clock. Compare stable-content fingerprints (excluding SQLite row IDs), source-to-output coverage, unresolved queues, and state-level worker/date/location summaries across two independent runs.

## Acceptance gates before replacing published data

- The bundle is complete enough for the intended historical coverage; omissions and stale state captures are enumerated rather than masked by the old database.
- All raw rows are accounted for as published notices, linked versions/duplicates, or explicit parse/identity/quality exceptions. Rejected or unresolved rows remain inspectable with their source artifact and reason.
- Date range, precision, location role, multi-site allocation, and worker-count checks pass on state-stratified fixtures and sampled original documents, including SC, GA, IA, KS, NJ, and CA edge cases.
- Deterministic portions replay byte-identically in a fresh environment with pinned dependencies and references. Any model-derived portion is represented by a versioned decision ledger and is independently auditable.
- The candidate DB passes integrity checks; exports and site views agree with it; state-level discrepancies are explained by source evidence. Old-DB count or key differences are reported, but are not by themselves failures.
- Promotion is staged and reversible. Only after the candidate passes review should the published dump, exports, and site be replaced and the branch merged to `main`.

## Current gap and model-call boundary

The `2026-09-23-ia-la-official.tar.gz` bundle adds Iowa's official current event-log workbook and 2018–2023 historical PDF to the Louisiana-augmented, mixed-time inputs. Every prior source-input hash is unchanged. The old-DB-derived `rebuild_policy.json` is intentionally omitted, so use this bundle in `--source-only` mode. The older `2026-09-22-ca-nj-refresh.tar.gz` remains available for legacy comparison. Source-only replay writes a checksummed, row-level exception manifest for rejected or unresolved current raw, historical raw, archive, BLN, and official agency rows. The Iowa official artifacts are preserved and verified; all 935 row observations are held in the exception ledger and none yet enter active notices. The manifest is an accounting and review queue, not a determination that every excluded row is a duplicate. Most raw state snapshots predate the CA/NJ refresh. The old database should be used to find possible missing evidence, not to decide which source rows are valid.

To recreate the expanded bundle from the previous frozen inputs, extract the September 22 bundle to a new scratch directory, then run `python -m warnlive.migrate.source_bundle create --workdir <extracted-sources> --agency-artifacts data/source_snapshots/la --ia-artifacts data/source_snapshots/ia --out <new-bundle>`. Both agency-artifact options check pinned bytes and applicable structure before writing the bundle. The resulting tarball is deterministic for the same input bytes.

The offline rebuild and export path should not make LLM calls. **Before any new or repeated LLM calls** for employer identity, entity resolution, place decisions, industry, or other classification, agree with the project owner on the exact task, input/evidence contract, prompt and model, evaluation set, acceptance thresholds, maximum call/cost budget, and how decisions will be reviewed and recorded. Running every decision again is acceptable if evaluation justifies it; it is not an automatic step of the rebuild.

An earlier mixed-time source-only replay produced 86,936 notices, 93,073 versions, 9,631 links, and 9,031,813 workers, with SQLite integrity `ok` and zero foreign-key errors. Its 37,953-row exception manifest includes one missing current raw source file (LA), 121 parse failures, 1,524 same-key conflicts, 301 occupied-source-month archive rows, 33 invalid BLN employer rows, 6,330 BLN rows classified as GA/SC/IA source-identity unresolved, 15,390 superseded BLN rows, and 14,253 unmatched rows after conservative gap filling. A later audit found that 555 Iowa BLN versions were actually ingested despite being classified identity-unresolved, so this earlier accounting report must **not** be used as proof of a consistent Iowa quarantine. Its generated manifest is at `/private/tmp/warn-source-only-20260922-07.exceptions.jsonl` locally and is not committed.

Louisiana's official 2025 and 2026 WARN PDFs are preserved under `data/source_snapshots/la/` with byte hashes, source URLs, and capture metadata. A strict table extractor accounts for 38 notice rows and one rescission annotation, retaining original cells, source-row pointers, an explicit date interval, and unverified address and worker-allocation roles. The reviewed source-only overlay now admits 31 official rows; six IDEA/SafeSource rows remain held and the rescinded UPS row is not active. The earlier 623-notice Louisiana candidate was BLN-derived and is preserved as a comparison, not a publication target.

Two pre-overlay replays of the expanded bundle retained the prior notice, version, and link fingerprints, with 86,936 notices, 93,073 versions, and no integrity or foreign-key errors. Their 37,992-row exception manifests have the same SHA-256 (`0bec8b0238c9614c3cbdc342d2b1a5f47e35ba74d436117b568f79c5f89e6ad9`), exactly 39 rows more than the prior bundle. A read-only exact notice-date/worker-count comparison to non-superseded LA BLN rows gives 17 official rows with one candidate, 18 with multiple candidates, and three with none. These signatures help prioritize review; they do not prove identity.

Regenerate the row-level Louisiana comparison with `python -m warnlive.migrate.la_reconcile --bundle data/source_snapshots/2026-09-23-la-official.tar.gz --out <new-report.json>`. It records both source checksums and, for every exact notice-date/worker-count BLN candidate, its original data-row ordinal, row hash, employer, location, effective date, superseded/amendment flags, and whether the effective start agrees. Two independent runs yielded the same report SHA-256 (`e039631303e1dc55c6adec08d5f0d30dead1876c97fa5cf397dc6c33f66b16e9`). The three rows with no active exact-signature candidate are 2026 UPS (`2026.pdf:p1:r13`), Mosaic (`p1:r14`), and Elevance (`p1:r15`). This is a comparison to the frozen BLN copy, not proof that the notices are absent from every source or that the other rows are duplicates.

The command also accepts `--db <candidate.sqlite>` for a read-only official-row-to-candidate coverage report. Its pre-overlay run found 29 official rows with one canonical date/worker signature, three with two, two with three, and four with none. Those buckets are diagnostic only: two IDEA official rows share the same signature. See `docs/la-source-review-2026-09.md` for the source-backed exceptions requiring curation before Louisiana can be promoted.

## Current deterministic checkpoint

The Louisiana overlay and an Iowa quarantine-alignment fix produce an isolated candidate of **86,388 notices, 92,492 versions, 9,493 links, and 8,998,760 workers**. Iowa falls from 913 to 375 active notices (57,710 to 27,053 workers) because 555 BLN-derived versions are no longer admitted under a rule that already labeled those source rows identity-unresolved. This is an explicit coverage gap, not evidence those historical notices are false. The remaining 38,018 exception rows are preserved with a stable checksum; SQLite integrity is `ok` and foreign-key errors are zero. Louisiana has 613 notices and 83,991 workers, including 31 official-PDF records; the IDEA/SafeSource and rescinded UPS rows remain out of active totals. The candidate is **not** publication-ready.

A read-only field-level audit of frozen historical raw CSVs found 54,814 rows sharing a candidate dedupe key. Among them, 6,562 rows disagree with the selected current version on at least one canonical field, including 3,827 worker-count and 3,404 effective-date disagreements; 78 source rows have no candidate key. These are review signals, not 6,562 errors: source age, intentional repairs (notably Illinois), and transcription differences can explain some. The audit must not automatically overwrite newer values. The source-only exception manifest's “represented by key” category does not by itself prove field-level agreement.

The read-only Iowa correspondence report (`python -m warnlive.migrate.ia_reconcile --bundle <bundle> --db <candidate.sqlite> --out <new-report.json>`) independently reproduced the same SHA-256 twice (`58ed7a43fb56c930ac3d936cfdf410745cdb542f7f6b19ee7e050484e8cea60a`). Among 1,300 BLN transcription rows, 315 are marked superseded. Of 985 active rows, 385 share a source transcription ID with raw, 3 have matching employer/date/workers/place, 1 matches those facts but disagrees on place, 14 match dates/workers only, and 582 have no raw counterpart under these exact tests. The 415 normalized raw rows yield 375 active candidate notices; 29 raw rows have no active BLN nomination. These buckets nominate review, not deduplication or admission. Crucially, the candidate has Iowa notice dates in 2011–2018 and 2026 but **none in 2019–2025**. Among the 582 BLN rows with no exact raw counterpart, 531 have notice dates in 2019–2025. This is a material coverage gap, not a reason to treat BLN transcription hashes as filing IDs. The current official Iowa event-log workbook says it covers notices since June 2021, while the frozen raw and BLN evidence reaches earlier years. The official recent log therefore cannot establish the identity of pre-2021 historical transcriptions. Its notice-level columns include address, city, county, notice type, employee count, notice date, and layoff date; source errors and revisions (including an invalid `3/32/2026` layoff date in the official PDF log and frozen 2026 raw CSV) need row-level review rather than silent correction.

The newly frozen current Iowa workbook has 573 dated observations from June 2021 through September 2026. The strict extractor preserves every physical row, typed cell, source hash, address state, and notice-type label; it does not assume each row is an additive event. A read-only comparison to the frozen raw and active BLN transcriptions yielded 65 official rows with an exact employer/date/workers/place raw counterpart, 344 with such a BLN counterpart, and 205 with no exact BLN counterpart. These categories overlap and do not prove filing identity; each candidate now carries its own match basis, including place disagreements within an otherwise matching facts group. The workbook contains 14 distinct effective-date phase rows for one 2026 CNH amendment, one exact duplicated row pair, and three addresses explicitly outside Iowa. Changed-date, worker-reduction, additional-worker, and generic amendments require distinct reviewed dispositions before they can affect active totals. The frozen historical Iowa agency PDF has now been projected into 362 row-level observations: 39 in 2018, 79 in 2019, 63 with valid dates in 2020, 35 in 2021, 98 in 2022, 47 in 2023, and one Sorenson row with a malformed printed notice date. Two printed rows have a city run into the address text; the extractor leaves city blank and flags the loss. Of those 362 historical rows, 282 have an exact employer/date/workers/place BLN counterpart and 57 have no exact BLN counterpart. The complete read-only Iowa report, now bound to an Iowa candidate-content fingerprint, reproduced byte-for-byte twice (SHA-256 `b5ab6ff02b3f323b10e8ec3544d307340c3898cf50bf36e32f4ca9d82a7cdec8`). This recovers the 2019–2020 years as source evidence, but the rows still need correspondence and amendment review before admission to the database.

The augmented source bundle independently rebuilt byte-for-byte twice (SHA-256 `54669ad8cc15565c155ed81ba2c5bd0581c89ba94a88c99b2244d318563e3e51`). It adds only the three Iowa artifact/manifest files; every previously bundled file retains its hash. An isolated source-only candidate replay using the augmented bundle retained the earlier notice, version, and link fingerprints and the 86,388/92,492/9,493 counts. This is expected because the official Iowa evidence is not yet admitted.

After adding all 935 official Iowa observations to the held-row ledger, two isolated augmented-bundle replays retained identical active notice/version/link fingerprints and totals. Their exception manifests each contain 38,953 rows (38,018 prior exceptions plus 935 Iowa official observations) with the same SHA-256 `b326bb720f747b9170b3d784ce1d7a6aaa0c8ebc67e077c78c8b659461e3f8a6`. SQLite integrity remains `ok` with zero foreign-key errors. This makes source accounting explicit without claiming the held rows are duplicate or invalid.

The two official Iowa captures overlap rather than forming disjoint annual files. On exact folded employer, notice date, layoff date, workers, and city, 135 historical-PDF rows have one current-workbook counterpart and four have multiple; 223 have none. The 223 include all 182 PDF rows from 2018–2020 (including the malformed-date row) plus 41 rows from 2021–2023 that need revision/coverage review. The matching observations should not be double-imported merely because they occur in both official artifacts.

Generate a compact, evidence-only adjudication queue with `python -m warnlive.migrate.ia_review --report <ia-correspondence.json> --out <new-worklist.json>`. With the candidate and bundle above, its 935 source-row entries include 168 reciprocal one-to-one, full-field cross-source nominations, 102 reciprocal one-to-one nominations with changed reported fields, four weaker same-employer/city nominations, 137 ambiguous nominations, and 524 without a cross-source counterpart. The queue also groups 184 employer/city histories and flags 16 anomaly rows. Its checksum is `e5ce87a12c1b583d53aeef28f7bd1d5722e9f2b70c6206aa1a28bfce413a0fad`. These counts classify observations in both directions, not unique WARN filings; neither the nominations nor the worklist authorize automatic merging. In particular, the 14 CNH rows with distinct layoff dates require phase identities, and the malformed Sorenson printed date requires a documented source-backed correction.

Two isolated replays after pinning the Illinois repair's version observation time to `--observed-at` match on notice, version, and link fingerprints and on the 38,018-row exception-ledger checksum (`9a1083fd3e181e53ba56fe958349ed0a86d1939487db11c123f6faaf397ee2bc`). Isolated CSV exports match byte-for-byte; each national CSV has 86,388 rows and 8,998,760 workers, equal to its database and site metadata. With `build-site --as-of 2026-09-22`, all 568 site files also match byte-for-byte across the two replays (sorted path/content tree SHA-256 `f79084a2e2ca18426bc529adafae89221e6dd60e574df957b732a6aba1fbefd8`). The prior wall-clock Illinois timestamps had caused all 256 notice-detail shards to differ despite identical stable DB fingerprints; that defect is now covered by a focused test. These checks establish deterministic replay in this environment, not yet a fresh-environment dependency lock or semantic correctness of every notice.

## Next implementation sequence

1. Obtain filing-level evidence for the held Louisiana IDEA/SafeSource rows and review the BLN-only Blue Cross row. Do not allocate multi-site totals or treat unverified addresses as worksites without source evidence.
2. Preserve and import official Iowa notice-level evidence for the missing 2019–2025 years where available; reconcile each row with BLN and raw without inventing a filing ID. Review and resolve other source-only archive/BLN overlap queues using source evidence or explicit curation decisions; keep the legacy policy only for read-only comparison. Finish source-aware identity for SC and GA; Kansas now keys fresh builds by its verified source record number.
3. Freeze a more contemporaneous capture where possible; declare intentionally historical/missing artifacts in the manifest. Verify two fresh, isolated database/export/site replays and sample source-to-output interpretation, not only row accounting.
4. Review unresolved classification and matching queues and a held-out sample with the owner. Only then decide whether to reuse, revise, or rerun LLM decisions.
5. Review the resulting candidate, stage replacement and deployment, then merge to `main` under the owner's Git identity and remove the temporary branch.

# Massachusetts live collector probe (2026-09-23)

The last saved production health result for Massachusetts was a September 22 HTTP 403 on the [official WARN index](https://www.mass.gov/info-details/worker-adjustment-and-retraining-notification-act-warn-layoff-and-closure-updates), following ten failed runs. On September 23 Eastern time, a read-only live probe from this environment received HTTP 200 with the existing `niquests` client; plain `requests` and `curl` also received 200. The index exposed five fiscal-year workbooks (FY22–FY26) and a weekly CSV ending September 18, 2026. The existing adapter fetched all six files, wrote 392 deduplicated raw rows, normalized all 392, and passed the configured verification checks. This demonstrates that the source path and current parser work from this environment. It does **not** prove the scheduled runner has regained access or that its optional Zyte secret is configured. No production health row was changed.

The diagnostic capture is pinned under [`data/source_snapshots/ma/2026-09-23`](../data/source_snapshots/ma/2026-09-23/manifest.json), with the index, six agency files, generated raw CSV, source URLs, byte sizes, and SHA-256 values. It is not in the national rebuild bundle or candidate database. The source table's `RECEIVED` field remains agency receipt, not legal employee notice; the 392-row normalization has no source-backed `notice_date` values.

Compared as a six-field raw-row multiset with the frozen `raw/ma.csv` (377 rows), the live capture has **16 added rows and one removed row**. The removed row reports `SDH Education East, LLC (aka Sodexo at Bentley University)` with receipt July 9, 2026, Waltham, effective August 11, and 173 workers. The added weekly row reports `SDH Education East, LLC (aka Sodexo)` with the same other five cells. Their normalized content keys differ (`97d9a1a374444381d76984dea25cb796f9388892` vs `e691489de93f3bde6876561f8a7c95b827cc432e`). The current local database already has both as separate Massachusetts notice IDs 27554 and 61693. This is direct evidence of a source-label revision and a duplicate *source-record representation* under the present content key. It does not by itself establish legal filing or layoff identity, and the two 173-worker counts must not be summed. The frozen candidate contains the older label only.

Before a fresh Massachusetts capture enters a replacement bundle or live database, establish a guarded correspondence for this source revision and inspect other same-cell label changes. Then rebuild in isolation and verify old-to-new notice lineage, worker counts, and date roles. A fresh scheduled-run health result is still required before changing the site's failed status. The live probe cannot substitute for that environment-specific check.

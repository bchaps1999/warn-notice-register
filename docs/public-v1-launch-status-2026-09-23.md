# Public v1 launch status against the September 23 audit

**Superseded release checkpoint:** the accepted v1.0.0 source-only dataset and
current release gates are documented in the
[September 24 release notes](release-v1-2026-09-24.md). The Missouri totals and
open statuses below describe the earlier audit point, before the Ohio recovery,
artifact promotion, and deployment checks. They are retained as audit history.

The [external launch audit](public-launch-audit.md) is the issue list. This page tracks findings against the shared, uncommitted `reproducible-warn-rebuild` working tree. **Nothing on this page authorizes publication.** Every gate must be rerun on the exact release commit. The current agency-only candidate is the [September 24 Missouri expansion](agency-mo-expansion-2026-09-24.md), with 70,636 notices; its Texas/Oregon and New York/Georgia source additions are documented separately. The 60,324-notice v4 transition and earlier v11/Illinois checkpoints below are historical comparisons. The checked-in national CSV still has 92,604 rows and its source-observation export is header-only.

| Audit item | Current status | Evidence or next gate |
| --- | --- | --- |
| P0 1. Promote one accepted artifact set | **Open** | The latest isolated agency-only candidate has 70,636 notices, 78,407 versions, and 7,367,863 reported workers. The prior Kansas 25,141-worker repair depended on BLN historical raw and is retired. Database, CSVs, health, site, and deployment still need a coordinated release. |
| P0 2. Source interpretation | **Open, partly bounded** | MA/KY receipt roles and RI ranges are corrected. The [agency-only transition](agency-only-rebuild-2026-09-23.md) removes BLN integrated rows, GitHub Flow historical raw, and BLN-hosted Texas/Ohio mirror rows. Illinois uses IEBS IDs and preserves its agency-report date separately from legal notice date. Subsequent direct-source rules admit New York annual rows, Georgia PDFs, Texas and Oregon workbooks, and Missouri agency tables. NJ fanouts, held IA/NY/LA evidence, RI `WARN Date` role, and remaining historical gaps still need explicit release dispositions. |
| P0 3. Source observations | **Open** | Checked-in CSV has zero data rows. The current 70,636-notice candidate database has 974 verified IA/LA observations: 31 admitted, 935 identity-unresolved, six event-unresolved, one annotation, and one rescinded. That ledger is deliberately scoped to IA/LA; the separate exception manifest accounts for other held source rows. Public copy states the scope. |
| P0 4. Collector health | **Open, one false warning fixed** | The [collector health audit](collector-health-release-audit-2026-09-23.md) records FL/MA/HI failures, CO/GA degraded states, saved causes, and release dispositions. CO's warning was only CSV column order; the verifier now accepts reordering. A [Florida direct-year patch](fl-collector-repair-2026-09-23.md) reproduces the prior cached capture but still needs a fresh live fetch. A [Massachusetts live probe](ma-collector-probe-2026-09-23.md) now fetches and verifies 392 rows, but scheduled access and a source-label duplicate remain open. The [Hawaii transition review](hi-source-transition-2026-09-23.md) includes an inactive adapter phase. GA/IA/KY/OR/TN collectors are now intentionally paused because upstream appends BLN histories; they need direct agency-only adapters before scheduled collection resumes. Repair or verify remaining adapters and capture release-day status before promotion. |
| P0 5. Python tests | **Code fixed; release rerun pending** | The full Python suite passes **321 tests** after agency-only guards, including cached-ingestion, bundle, and direct-rebuild bypass checks. Run it again on the exact release commit. |
| P0 6. Clean release commit/replay | **Open** | Earlier v11 and Illinois replays tested BLN-containing policy. The latest Missouri bundle matched across two corrected isolated DB replays, including counts, fingerprints, exception checksum, integrity, and the SQLite file hash. A local site preview built from that candidate, but final CSV/site/dump reconciliation, clean code revision, and target-platform replay remain release gates. |
| P0 7. Pinned production install | **Code changed; target validation pending** | `install.sh` now requires Python 3.13, installs the hash-checked rebuild lock, installs SHA-pinned upstream repos without dependency resolution, and runs `pip check`. All five Python workflows request 3.13. A local no-index dry run of the lock passed; fresh Ubuntu installation and replay are still required. |
| P0 8. Fail closed on restore | **Code fixed; CI rerun pending** | Daily, weekly, and ad-hoc workflows no longer suppress `unpack-db` errors. Restore validates integrity, foreign keys, core schema version, and a nonempty notice table before replacement. It rejects existing SQLite WAL/SHM sidecars, because an active connection can otherwise keep serving old data after a successful file swap. Six focused tests pass, including legacy gzip migration and an active-WAL regression; the current SQL dump restored to an isolated path with integrity `ok` and zero foreign-key errors. |
| P1 9. Version metadata/tag | **Open** | Retain `0.1.0` until the accepted release commit is ready; then align Python/site metadata and tag that commit. Internal schema and notice revision numbers are separate. |
| P1 10. Internal pilot files | **Open** | Decide the public evidence set and move retired model experiments out of the release tree without deleting source provenance. Rewrite top-level README for the accepted artifact set at promotion. |
| P1 11. Public Git history | **Product decision open** | Curating a new public repository/history may be appropriate, but do not rewrite the current working branch while concurrent work and release evidence are unfinished. |
| P1 12. Build artifacts | **Partly fixed** | `.DS_Store` and `*.tsbuildinfo` are ignored and the two tracked files were removed from the index while working copies remain. Review untracked `output/` and historical reports before staging. |
| P1 13. Site setup instructions | **Code fixed** | Site README now uses `warnlive unpack-db` and no longer says the repository is private. Verify again on the release commit. |
| P1 14. Methods download/copy | **Code fixed; release check pending** | Methods links directly to the notice CSV and compressed SQL dump and gives the restore command. It describes conservative source relationships and coverage gaps and avoids treating current parent annotations as historical ownership. Confirm links and copy against the promoted artifacts. |
| P1 15. Legacy compatibility | **Partly verified** | The restore path accepts a version-4 legacy gzip and migrates it to current schema in a regression test. Old observation-column migration remains covered separately. Audit other compatibility paths before removal. |
| P1 16. Pilot/unused UI code | **Partly fixed** | Removed the unused `DefRow` component. Review temporal pilot placement after deciding whether its dated ownership evidence remains part of the research record. |
| P2 17. Frontend tests | **Code fixed; CI rerun pending** | Added two meaningful data-routing/cache tests; `npm test` and `npm run build` pass locally. The test workflow now runs `npm ci`, tests, and build. |
| P2 18. Bundle size | **Open, measured at build** | Vite still warns about main and county-map chunks above 500 kB. Check actual deployed load performance before deciding whether to split them. |
| P2 19. Static copy/counts | **Partly fixed** | Removed the hard-coded “44 state portals” description and the README's approximate count. Review title, social metadata, and live totals after final build. |

The immediate release path is: explain the material source/coverage deltas; make an accepted artifact set from one frozen candidate; verify its CSV/observation/exception/site/dump reconciliations; run a clean checkout on the target platform; then promote and deploy the same release. The previous artifacts must remain available for rollback.
